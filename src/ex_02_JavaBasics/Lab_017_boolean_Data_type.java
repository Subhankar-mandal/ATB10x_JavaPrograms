package ex_02_JavaBasics;

public class Lab_017_boolean_Data_type {
    /** Boolean data-type represents a logical value.It may be either true or false.
     * Conceptually it represents a single bit (0 -> false, 1 -> true) of information
     * but the actual size used by JVM is implementation dependent.
     * Size : JVM dependent (typically 8 bits, 1 Byte)
     * */
    public static void main(String[] args) {
        boolean isHappy = true;
        System.out.println(isHappy);
    }
}
